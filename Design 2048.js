// Gameplay:
// 1 player
// 4x4 board
// Combine numbered tiles in order to gain a higher numbered tile
// Start with two tiles, the lowest possible number available is two
// You will play by combining the tiles with the same number to have a tile with the sum of the number on the two tiles.

// Controls:
// upward, downward, and sideways.

// Rules:
// You just need to move the tiles and every time you move one, another tiles pops up in a random manner anywhere in the box.
// When two tiles with the same number on them collide with one another as you move them, they will merge into one tile with the sum of the numbers written on them initially.

// class Game {
//   constructor(size = 4) {
//     this.board = Array(size).fill(Array(size));
//     this.isWin = false;
//   }
// }

// console.log(new Game());
